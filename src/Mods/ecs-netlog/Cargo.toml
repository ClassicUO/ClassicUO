# Standalone: not part of the repo-root Rust workspace.
[workspace]

[package]
name = "ecs-netlog"
version = "0.1.0"
edition = "2021"

[dependencies]
wit-bindgen = "0.46"
serde_json = "1"
base64 = "0.22"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "s"
strip = true
